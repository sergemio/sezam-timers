# Kitchen Timers — Sezam&Co
> Last update: 2026-07-19
> 📦 Ce repo = **code du prototype webapp** du projet **Timer App** (hub de suivi : `P/topics/timer-app/README.md`). Terrain de prototypage UX avant le build **Flutter**.

App de minuteurs cuisine pour tablettes (Android + iPad, web app). Fichier unique : `index.html`.

## Décisions validées (12/07/2026)
- Voix en **anglais** (Web Speech API, en-US), phrase éditable par timer (défaut : "The {name} is ready")
- Après arrêt d'une alarme → le timer revient à sa durée initiale
- Hébergement cible : GitHub Pages (pas encore fait) ; v2 : sync Firebase
- Persistance : localStorage (`sezam-timers-v1` défs, `sezam-timers-run-v1` état en cours)

## Fonctionnalités v1
- CRUD timers (nom, durée, ringtone ×3 synthétisées WebAudio, phrase vocale)
- Grille auto-scale : les tuiles se redimensionnent pour que tout tienne sans scroll
- Tap = start ; tuile en cours : boutons −10s / +10s / ✕ (tap sur le corps ignoré, anti fausse manip)
- Alarme : ringtone + voix, répétition à ~7s puis fréquence croissante (gap ×0.72, min 2s), compteur de dépassement (+m:ss), pulsation rouge
- Mode Edit : wobble, tap = éditer, drag pointer-based = réordonner
- Timestamps (endsAt) → survit au reload / onglet en arrière-plan ; Wake Lock ; unlock audio au 1er tap ; vibration si supportée (Android oui, iPad non)

## Lancer en local
```
python -m http.server 8734 -d C:\Users\serge\Claude\topics\kitchen-timers
```
→ http://localhost:8734

## Design
Thème **light** (demandé par Serge) : fond papier chaud `#f5f1e8`, texte charbon, accent ember `#e8600f`. Fonts : Big Shoulders Display (titres/noms) + Chivo Mono (chiffres, tabular).

## Changelog
- 2026-07-21 — **[Logo V3 + wordmark] Nouveau logo « 0 » 7-segments et retour de l'ancienne typo** (retour Serge : le DSEG14 du header illisible ; logo définitif à venir de sa main, V3 = provisoire validé) — le chrono SVG du header remplacé par un **« 0 » de police 7-segments** (6 segments à coupes 45°, encoches à mi-hauteur) racontant le « quart de tour » : demi-segment haut droit + segment haut-droit **grisés** (45 % de l'encre via `opacity`, marche sur tout fond) + **barre ember verticale** intérieure. SVG inline statique 8 polygones, segments en `currentColor`. Wordmark : `Cadence` en DSEG14 → **`CADENCE` en Big Shoulders Display 800 majuscules** (1.42rem, 1.24rem sous 470px). Mark en `height:26px;width:auto` (portrait, largeur par aspect-ratio → jamais hors header, centré verticalement par le flex existant, le mot part de son milieu). Même traitement appliqué à la **landing** (`timer-app/landing/index.html`, non poussée) : mark défini une fois (`g#cadence-mark`) + `<use>` pour seal et footer, `CADENCE` majuscules — vérifié par sonde 9/9 (use résolus, aspect portrait, mark dans la nav). App vérifiée par captures 1024/440px. Maquettes de travail : `_cadence-harness/logo-mock.html`. **Poussé le 2026-07-21 — commit `9ec2110` (SW v24).**
- 2026-07-21 — **[Edit mode visible] Fin de la confusion « je suis en édition sans le savoir »** (retour terrain Serge : l'équipe restait en mode édition sans s'en rendre compte — badge texte minuscule, wobble ±0.5°/2.6s qui « flotte », tuiles identiques au mode normal, SW `v23→v24`). 3 signaux redondants (couleur + mouvement + libellé) : **(1) Jiggle iOS** — `wobble` passe de 2.6s/±0.5° à **0.32s/±1.2° + translateY ±0.5px**, phases décalées conservées (délais recalés -.11s/-.22s), garde `prefers-reduced-motion` ajoutée (l'ancien wobble n'y était pas). **(2) Badge → pastille pleine** — `✎ EDIT` texte nu ambre 5cqh → **pill fond ambre/texte blanc**, radius 999px, ombre, planchers `max(5cqh,11px)` / paddings `max(...,5px/9px)` pour rester lisible sur petite tuile. **(3) Tuile « éteinte »** (option A validée, pas de fond de page teinté) — voile crème `rgba(244,239,228,.58)` en `.inner::after` (z3, sous le badge z4) qui délave nom/chiffres/camembert **sans arrêter l'affichage** (les chiffres tournent sous le voile → pas d'ambiguïté « timer arrêté ? ») + **contour pointillé gris** `2px dashed` offset -6px (langage « objet éditable », le dropTarget du drag repasse ambre via override) + phaseBanner à 45%. **Exemptions** : tuile `ringing` sans voile (une alarme doit rester criante même en édition) ; clones ×N sans jiggle ni badge (inertes, règle existante). Zéro risque de flash : l'état vit sur `body`, pas de course d'attributs au rebuild. Vérifié headless **18/18** (o.html : jiggle actif/rapide, pill ambre+planchers, voile+z-order, outline, tick sous voile, clone inerte, exemption ringing, sortie propre) + capture visuelle 1024px conforme. **Poussé le 2026-07-21 — commit `9ec2110` (SW v24).**
- 2026-07-19 — **[Touch targets] Boutons ±10s/✕ visables sur petites tablettes** (retour Serge : tuiles compactées par les batchs ×2 → boutons trop petits/trop ronds à viser, SW `v22→v23`) — 3 mesures complémentaires : **(A) radius plafonné ET proportionnel** — `border-radius:13px` fixe devenait une gélule sur petit bouton ; → `min(10px,2.6cqh)` : arrondi doux sur grande tuile (~10px), forme rectangulaire aux coins adoucis sur petite (~3-6px). **(B) plancher tactile** — les tailles en cqh/cqw n'avaient AUCUN plancher (bouton ~25px sur tuile de 150px) ; → `font-size:max(8cqh,13px)`, `padding:max(2.4cqh,6px) max(4.8cqw,10px)`, **`min-height:42px`** : le contenu scale, les cibles tactiles non (standard ~44pt Apple/48dp Material). **(C) hitbox invisible étendue** (conservatrice, demande Serge) — pseudo-élément `::after` débordant du visuel : **7px** sur les côtés extérieurs/verticaux, **2px** seulement entre +10s et −10s (le milieu du gap garde la frontière), **0px** sur le flanc intérieur du ✕ (le gap 8cqw isolant le stop destructif = buffer de sécurité voulu, intact). Vérifié headless **27/27** sur 3 tailles (1280/900/500) en grille dense 9 tuiles/5 actives : plancher 42.0px exact, radius ≤10, aucun chevauchement boutons/heure, hit-testing (±4px hors bouton → touche ; 3px côté gap du ✕ → ne touche PAS), +10s fonctionnel. Captures 1280 + 500 (vraie largeur — piège headless : `--window-size=500` donne un viewport de 534px, la « coupe » du 1er PNG était un artefact de capture, grille saine). *(Harnais : mesurer APRÈS la fin des anims d'apparition — `ctlIn` scale .94 faussait la hauteur.)* **Fix « obvious issue » (retour Serge, capture téléphone ~395px)** : le plancher 42px était ABSOLU — sur une tuile de ~170px de large, les 3 boutons planchers (~155px cumulés) débordaient de la tuile (✕ rogné au bord, +10s mordant le coin). Correction par **paliers container-query** (la tuile est déjà `container-type:size`) : tuile **>240px** = planchers pleins (42px/13px) ; **150–240px** = planchers compacts (`38px`/12px, paddings 5/7px, `min-width:auto`, gap 2cqw, marge ✕ 4cqw) ; **≤150px** = retour au pur proportionnel (une tuile minuscule ne peut physiquement pas loger 3 cibles de 42px — la hitbox `::after` et le tap-partout-pause restent). Vérifié headless **16/16** sur 5 tailles (395 repro Serge, 395 dense, 500/900/1280 dense) : rangée + ✕ **entièrement dans la tuile**, jamais sur l'heure, palier correct par largeur (179px→38h, 232→38, 284/305→42) + capture 395px conforme. **Poussé le 2026-07-21 — commit `9ec2110` (avec l'edit mode + logo, SW v24).**
- 2026-07-12 — v1 initiale (single-file) thème light, servie en localhost
- 2026-07-12 — fix: overlay `#empty` (position:absolute inset:0) interceptait tous les clics sur les tuiles car `#empty{display:flex}` (spécificité #id) écrasait l'attribut `hidden`. Ajout `#empty[hidden]{display:none}` + `pointer-events:none`. Vérifié headless (idle→running au clic). + favicon inline (supprime 404).
- 2026-07-12 — batch retours Serge (tous vérifiés headless, 0 erreur) :
  - **Voix féminine** : `scoreVoice()` sélectionne une voix anglaise féminine par nom (Zira/Aria/Samantha…), évite les voix masculines (David/Mark…). Machine actuelle → Zira.
  - **File vocale** (`speechQueue`) : les annonces prennent leur tour au lieu de se couper ; arrêter un timer ne coupe que SA voix, pas celle d'un autre timer encore en train de sonner.
  - **Interaction timer actif** : tap simple = **pause/reprise** (statut `paused` : temps figé, anneau gris, respiration) ; **double-tap** = **reset**. Désambiguïsation via fenêtre 260 ms.
  - **Anneau** épaissi (`--ring-w:10px`, `--tile-r:20px`).
  - **Bouton ✕** écarté des ±10s (`margin-left:10cqw` ≈ 36px) + bordure rouge distinctive (anti fausse manip).
  - **Nom du plat** agrandi (11cqh → 14cqh) ; décompte reste dominant. Vérifié en capture (nom long « Chicken Shawarma » non tronqué).
- 2026-07-12 — **timers par défaut** (SEED_VERSION=2) = Fries 4:00 · Mozza/elastics 2:15 · Manoushe 0:45 · Crispy chicken 6:00 · Dough making 12:00 · Cooking chicken 6:00 · Donut 1:30. Bump SEED_VERSION = ré-applique la liste (remplace les timers existants une fois).
- 2026-07-12 — **PWA iPad/Android** : manifest.webmanifest (standalone, paysage), icônes PNG 180/192/512, service worker (`sw.js`, offline shell), meta iOS (apple-touch-icon, status-bar, title), theme-color, viewport-fit=cover + safe-area, anti-zoom/anti-callout, layout robuste au padding. Haptique : Android via Vibration API ; iOS via hack `<switch>` (best-effort, **non garanti** — iOS Safari n'a pas d'API Vibration). Vérifié headless (7 timers, manifest valide, SW enregistré, comportements OK, 0 erreur).
- 2026-07-16 — **[PROTO] Timers enchaînés multi-étapes** (contexte : projet d'app pro à publier sur stores avec Henry ; stack cible décidée = **Flutter**). Un timer peut porter plusieurs **phases** nommées ; chaque phase décompte, à chaque transition l'app **annonce vocalement la phase suivante** + son de transition (montée E5→B5) + flash orange + haptique ; la dernière phase déclenche l'alarme complète. Éditeur de steps dans la modale (toggle Single/Multi-step, add/remove, min:sec par phase). Pastille `i/n` + nom de phase sur la tuile ; anneau de progression **par phase**. Rattrapage après veille sans spam vocal. Exemple par défaut *Steak · med-rare* (Saisir A → Retourner/saisir B → Repos) ajouté en **non-destructif** (n'écrase PAS les timers existants ; flag `sezam-steak-demo-v1`). Tag de version `#ver` dans le header (aide au proto). SW cache `v3→v4`. Vérifié : syntaxe OK, 9/9 câblage, 16 assertions logiques (progression, timing, alarme finale, catch-up). **Absorbe l'idée cuisson** = presets calibrés par le chef (l'app répète SES phases/temps, n'invente aucun temps). **Pas encore poussé sur GitHub Pages** (attente OK Serge).
- 2026-07-16 — **[PROTO v2] Batch retours Serge** (testé local d'abord, cf. [[feedback_local-test-first-then-push]]) : **(1) i18n FR/EN** switchable via bouton header (défaut **FR**, persisté `sezam-lang`) — chrome traduit par attributs `data-i18n`/`data-i18n-ph` + fonction `tr()` (renommée pour éviter la collision avec la var `t`=timer), voix TTS suit la langue (`fr-FR`/`en-US`, voix féminine préférée), phrase par défaut + démo Steak localisés. **(2) Timers à étapes visuellement distincts** : cadre ember permanent (`inset box-shadow`) + **bandeau de phase proéminent** en haut (`⛓ 2/3 · Repos`) au lieu de la petite pastille. **(3) Progression « camembert »** : la tuile entière se remplit (conic-gradient plein depuis 12h) et se vide en révélant le fond `--track` — bien plus lisible que le fin contour ; couleur `--fill` mint→amber→red selon l'urgence ; idle = panneau plat (contraste avec les tuiles qui tournent). **(4) Boutons ±10s plus gros** (8cqh, min-width 26cqw, fond solide + ombre). Vérifié : syntaxe OK, 0 collision `t()`, 0 référence orpheline, câblage i18n+visuel complet. Tag `proto · steps v2`. **Toujours pas poussé** (attente validation visuelle Serge+Henry).
- 2026-07-16 — **[PROTO v3] Corrections retours Serge** (captures headless Edge à l'appui : `--headless=new --screenshot`, servi en local) : **(a)** bandeau de phase — retiré l'emoji ⛓ (glyphe cassé « 8 » en police mono) + restructuré en `pastille 2/3` + `nom de phase` qui **tronque proprement** (ellipsis) au lieu de déborder ; **(b)** démo Steak passée en **français** (« Saisir → Retourner → Repos », noms courts) via migration `demo-v2` qui remplace l'ancien démo anglais → la **voix parle enfin français** ; **(c)** champs **min/sec** du modal élargis (`5.5ch`) — « 00 » n'est plus coupé ; **(d)** **header responsive** (`@media 820/470px`) : masque version-tag / « kitchen » / horloge et réduit les boutons sur mobile → « + Nouveau » ne disparaît plus (vérifié capture 490px) ; **(e)** **drag-to-reorder réparé** — 2 bugs : `touch-action:none` sur tuiles en édition (le tactile volait le geste) **et** exclusion de la tuile déplacée du `elementFromPoint` (elle se retrouvait sous le doigt → aucune cible détectée → réorg impossible même à la souris) + handler `pointercancel`. SW cache `v4→v5`, tag `proto · steps v3`. Vérifié : syntaxe OK, câblage complet, captures header OK. **Poussé LIVE le 16/07** (commit `80d43e6`, GitHub Pages) après OK Serge.
- 2026-07-16 — **[PROTO v4] Nom d'étape allongé** : `maxlength` du champ nom de phase **16 → 60** (le champ était trop court, phrase de cuisson coupée à 3-4 mots — retour Serge screenshot). Permet ~8-10 mots (aligné sur le champ « annonce vocale ») → cue parlé plus riche à la transition. SW cache `v5→v6`, tag `proto · steps v4`. Poussé (commit `dbce9a2`).
- 2026-07-16 — **[PROTO v5] Ordre boutons ±10s inversé** (retour Serge) : `+10s` désormais **à gauche**, `−10s` à droite (ordre visuel dans `.tctl` ; `data-a` inchangés, logique identique). SW cache `v6→v7`, tag `proto · steps v5`. Poussé (commit `0238023`).
- 2026-07-16 — **[PROTO v6] Libellé FR raccourci** (retour Serge) : sous-texte tuile au repos « appuyer pour démarrer » → **« démarrer »** (FR uniquement ; EN « tap to start » inchangé). SW cache `v7→v8`, tag `proto · steps v6`. Poussé (commit `54055fb`).
- 2026-07-16 — **[PROTO v7] Couleur de tuile interpolée en continu** (retour Serge, inspiré du rendu de la landing page) : `fillFor(p)` ne renvoie plus 3 paliers (`mint`/`amber`/`red` qui snappent) mais **interpole la couleur en RGB en continu** selon la fraction restante `p` — mint `#5cc79a` → amber `#edb24e` → red `#ec6a6a`, ancres aux anciens seuils (ambre pur à p=0.35, rouge pur à p=0.15), continu aux jonctions. Recalculée à chaque tick (150 ms) → la tuile « se dégrade » doucement au fil du décompte. Vérifié : node (continuité 0.35/0.15 OK) + rendu visuel comparatif (continu vs paliers). SW cache `v8→v9`, tag `proto · steps v7`. **Poussé** (commit `f3d9a5e`).
- 2026-07-17 — **[PROTO v8] Batch retours Serge** : **(a) camembert corrigé** — le remplissage utilisait `from -90deg`, or le `0deg` d'un `conic-gradient` CSS est **déjà à midi** → la référence était décalée à **9h** (« le zéro à 9h »). Passé à **`from 0deg`** : le vide s'ouvre à 11:59 et tourne en sens **anti-horaire** pour se refermer à **midi (0:00)**. **(b) libellé pause raccourci** : FR `❚❚ en pause · reprendre` → **`❚❚ reprendre`**, EN `❚❚ paused · tap to resume` → **`❚❚ tap to resume`** (le glyphe pause suffit, plus de retour à la ligne, on gagne de la place). **(c) timer par défaut** `Mozza or elastics` → **`Mozzarella sticks`** (+ phrase vocale « The mozzarella sticks are ready »), avec **migration non destructive** (flag `sezam-mozza-fix`) qui renomme le timer existant sur l'appareil sans toucher au reste. SW cache `v9→v10`, tag `proto · steps v8`. Poussé (commit `4d56171`).
- 2026-07-17 — **[REBRAND → Cadence]** (retour Serge — aligner l'app sur la [landing](../timer-app/landing/index.html)) : header = **logo timer (cadran + part ember) + « Cadence » + « minuteur cuisine pro »** (remplace « SEZAM·TIMERS / kitchen ») ; **nom Cadence partout** (`<title>`, manifest PWA name/short_name + `lang:fr`, titre iOS) ; **favicon** SVG = la marque timer ; **icônes 180/192/512 PNG régénérées** (chrono sombre fond `#1d211e` + part ember, produites via rendu headless Edge + resize Pillow) → icône d'accueil iPad/Android ; **fond** aligné sur le site (`--bg #f5f1e8→#f4efe4` + `theme-color`) ; **tag « proto · steps v » retiré** ; **horloge centrée** dans le header (position absolue) + affichée dès le chargement (`tickClock()` initial). SW cache `v10→v11`. Poussé (commit `5e160f2`). *(Plus de tag de version visible — suivi via git/changelog.)*
- 2026-07-17 — **Fond 100 % identique au site** (retour Serge) : retrait des halos radiaux de coin (`body::before` ember/menthe) → fond **plat `#f4efe4`** uniforme comme la landing (grain subtil conservé). SW cache `v11→v12`. Poussé (commit `eb2d6a6`).
- 2026-07-18 — **[Sonnerie par zone]** (différenciateur du cahier des charges) : **pivot majeur — le son appartient à la ZONE, plus au timer**. Nouvelle entité `zones` persistée (`sezam-zones-v1`), chaque zone = `{nom, sonnerie, couleur}`, pré-remplie **Chaud/Froid/Four/Pâtisserie**. **Gestionnaire de zones** (bouton « Zones » header) : renommer, choisir la sonnerie (avec ▶ écoute), recolorer (clic pastille), ajouter, supprimer. La modale d'un timer choisit désormais une **zone** (chips + « Aucune »), plus une sonnerie ; l'alarme joue `soundFor(t)` = son de la zone (fallback legacy `tone` pour les timers non migrés). **6 sonneries synthétisées distinctes** (Bell/Beep/Chime/Ping/Buzz/Marimba). **Pastille couleur + nom de zone sur la tuile**. **Pas** d'annonce vocale de zone (choix Serge). **Migration non destructive** : timers existants inchangés (gardent leur son) jusqu'à assignation ; 4 zones ajoutées. Vérifié : syntaxe (`new Function`), rendu headless des 2 modales + pastille tuile. SW cache `v13→v14`. Poussé (commit `290c9a6`).
- 2026-07-18 — **Header foncé** (retour Serge — matcher le header du cahier des charges) : fond du header `#191b14` + filet inférieur clair (à la place du dégradé blanc) ; logo crème + part ember, « Cadence » blanc / « — Kitchen Timer » gris crème ; horloge en gris clair ; **boutons blancs conservés** (option A) + Nouveau ember. `theme-color` → `#191b14` (barre de statut mobile fondue avec le header). **Corps de l'app inchangé** (header foncé / corps clair, comme le document). SW cache `v14→v15`. Poussé (commit `91b25e9`).
- 2026-07-18 — **Timers réels du resto hardcodés** (remplacent les placeholders, en attendant que l'app soit autonome) : dans l'ordre de la photo de Serge — **Manouche 0:45, Mozzarella sticks 2:15, Fries 4:20, Crispy 6:15, Melt cheese 3:30, Dough 12:00, Cook chicken** (3 étapes : Cuire 6:00 → Retourner 1:30 → Cuire 6:00 = **13:30**). Aucune zone assignée (section inconnue par timer → laissée vide, comme demandé). Phrases FR. **`SEED_VERSION` 2→3** = ré-applique le jeu une fois (**remplace les timers existants** ; les zones sont conservées). SW cache `v15→v16`. Poussé (commit `42e2bf3`). *(Note orthographe : photo « MANOUCHE » → « Manouche » ; « Pâte » renommé « Dough » par Serge pour l'alignement EN.)*
- 2026-07-18 — **Zones hardcodées + défaut EN + call-outs EN** (retour Serge, `SEED_VERSION 3→4`, SW `v17`) : **zones à ids stables** (`chaud/four/fritteuse/hello`) semées avec les timers → chaque timer **pré-assigné à sa zone** (répartition Serge : Manouche→Four, Mozzarella/Fries/Crispy→Fritteuse, Melt cheese→Four, Dough→**Hello**, Cook chicken→Four). **Langue par défaut = anglais** (défaut `'en'` + reseed force `'en'` + re-pick voix EN). **Call-outs en anglais** (phrases + étapes Cook chicken « Cook/Flip/Cook ») pour que la voix EN ne prononce pas du FR ; switch FR = l'utilisateur réécrit les phrases. **Reseed remplace timers ET zones** (les **sons des zones repassent aux défauts** — inconnus de moi — ajustables dans le gestionnaire). ⚠️ Zone « **Hello** » (assignée à Dough) ressemble à un nom de test — à confirmer/renommer. Vérifié headless (EN + pastilles). Poussé (commit `9a0cdb5`).
- 2026-07-18 — **Fixes terrain (test tablette Lenovo au resto)** (retour Serge, SW `v17→v18`, commit `3ced909`) : **(1) orientation** — `manifest` `"landscape"` → **`"any"`** (l'app **installée** en standalone était verrouillée en paysage ; en navigateur c'était OK car le manifest est ignoré). ⚠️ sur un appareil déjà installé, il faut **désinstaller/réinstaller** le raccourci pour relire le manifest. **(2) volume trop faible** — les sonneries synthétisées plafonnaient à ~0.5 sans étage master ; ajout d'un **master gain + limiteur** (`DynamicsCompressor`) + **`TONE_BOOST=2.0`** (le clic compensé à .12) → alarme nettement plus forte sans saturer. Plafond restant = web sur canal média (pas d'accès au canal alarme Android) → levé seulement au build natif/Flutter. **(3) horloge header** en **DSEG7** (7-segments type Casio, embarqué data-URI), **blanc `#fff`**, agrandie (`.85rem`→`1.3rem`).
- 2026-07-18 — **Typo LCD : header « Cadence » en DSEG14, chiffres des tuiles en DSEG7** (retour Serge validé sur [labo interactif](https://claude.ai/code/artifact/b0e5f6cf-c8c3-4641-b5f6-fc0823bf2967), SW `v18→v19`, commit `3e4df77`) : header `.logo b` → **DSEG14 Classic** (LCD alphanumérique lisible ; « — Kitchen Timer » reste en Big Shoulders). Tuiles `.time` → **DSEG7 Classic** avec un **fantôme de segments éteints** (`.tghost` = `88:88` en **`rgba(0,0,0,.05)`**, `text-shadow:none`) synchronisé dans `updateTiles` → segments éteints **toujours un poil plus foncés que le fond**, même sur le camembert coloré (choix « V3 » ; l'idée « nom du plat en LCD » a été **abandonnée**, jugée peu lisible). DSEG14 embarqué en data-URI. Vérifié headless (idle + running). *(Nettoyage : `wm_compare.html` temporaire retiré du repo, commit `3d7e8c7`.)*
- 2026-07-18 — **Réglages LCD + responsive** (retour Serge, SW `v19→v20`) : chiffres des tuiles **~15 % plus petits** (`.time` et `.tghost` `26cqh`→**`22cqh`**) pour regagner de l'air entre les éléments (le badge de zone touchait presque l'heure) ; **segments éteints encore plus clairs** (`.tghost` `.05`→**`.035`**). **Responsive header** : le wordmark DSEG14 étant plus large, palier `@media (max-width:960px)` qui masque « — Kitchen Timer » (garde « CADENCE » seul) en bande 821–960px ; ≤820px tout le wordmark reste masqué (icône seule). Vérifié headless à 1024/900/800 (0 chevauchement) ; tailles en `cqh` = responsive natif à toute taille de tuile.
- 2026-07-18 — **Indices « tap to start/pause » retirés + heure recentrée** (retour Serge, SW `v20`, même lot) : suppression des libellés `.sub` (`display:none` — « people understand that »). `.inner` passé de flex à **grille `1fr auto 1fr`** avec l'heure en **rangée centrale** → l'heure reste **dead-center** quels que soient les boutons +10/−10/✕ (rangée 3, `display:none` au repos) → **plus aucun saut visuel** repos↔lancé (vérifié : heure alignée entre tuile au repos et tuile lancée). Nom+badge encapsulés dans `.topg` (rangée 1). JS inchangé (les écritures sur `.sub` masqué restent sans effet). **Répartition (retour Serge)** : titre, heure et boutons en `align-self:center` dans leurs 3 rangées → chaque élément flotte au centre de sa zone (haut / milieu / bas) au lieu de se coller à l'heure → ça respire, réparti à toute taille de carte.
- 2026-07-19 — **[×2 Batches] Dédoublement de timer** (dernière feature avant la traduction Flutter, SW `v21→v22`) : cas réel = 2 friteuses partent avec le même plat, le timer est déjà pris. **Bouton `×N` à côté du titre** de toute tuile **active** (running/paused/ringing) : le presser **clone le timer** (même nom/durée/zone/étapes) qui **démarre de zéro immédiatement** (= nouveau batch) et s'insère **juste après son parent** dans la grille. Le label suit le compte (`×2` → `×3`), **plafond 3 par plat** (bouton masqué au cap, gel du label pendant le fade-out — protège la lisibilité de la grille). **Clones éphémères** : jamais idle — tout retour à idle (tap en sonnerie, ✕, double-tap reset) les **dissout** (tuile disparaît, rejoint le parent) ; ils **survivent au reload** (clé `sezam-clones-v1`, réconciliés au boot : parent vivant + run actif exigés) ; éditer/supprimer le parent dissout ses batchs ; **inertes en mode édition** (ni éditables ni draggables ni cibles de drop, pas de wobble/badge). Architecture : `timers` reste parents-only ; liste d'affichage `view` = `allTimers()` (parents + clones matérialisés `{...parent, id, parentId}`) reconstruite à chaque `render()`, moteur (`tick`) et `layout()` itèrent `view` ; le click-handler lit `tile._t` (les clones ne sont pas dans `timers`). **Animations anti-glitch** (retour Serge « pas de saut d'une frame à l'autre ») : bouton ×N replié à largeur 0 → **le titre glisse en douceur** pendant le fade-in (transition max-width/padding/opacity/scale .3s) ; **±10s/−10s/✕ fondent** aussi à l'apparition/disparition (opacity+translateY, plus de `display:none` sec — layout inchangé, l'heure ne bouge pas) ; **tuile clonée** apparaît en fade/scale-in (`spawnIn` .3s) ; gardes `prefers-reduced-motion`. Vérifié headless : **31/31 PASS** (spawn, insertion après parent, départ plein temps, labels ×3 partagés, cap 3, persistance reload, dissolution en sonnerie/double-tap, removeClonesOf, clone chaîné 1/3, mode édition inerte, régression parents pause/resume/±10s/stop/double-tap) + 3 captures (running ×2, 2 batchs ×3, cap 3). **Fix bug flagrant (retour Serge, test PC)** : à chaque tap, les boutons flashaient une fraction de seconde sur TOUTES les autres tuiles — cause : `render()` reconstruit toute la grille, `layout()` forçait un calcul de style AVANT que `updateTiles()` pose `data-status` → les transitions CSS jouaient visible→caché sur les tuiles idle (et symétriquement le fade-in de la tuile démarrée ne jouait jamais). Correction structurelle : **transitions supprimées** sur `.tctl`/`.dupBtn` (mortes-nées : tout changement d'état passe par un rebuild), état posé **avant** tout flush de style (`updateTiles()` avant `layout()`), et les apparitions voulues = **animations one-shot ciblées** sur la seule tuile qui change (`.justOn` → keyframes `ctlIn` + `dupIn` posées par `startTimer` via helper `animTile`). Re-vérifié : **37/37 PASS** (dont harnais anti-flash : opacité des tuiles idle = 0 exact sur 8 frames après start/pause/spawn, `justOn`/`spawnIn` posés puis retirés) + capture post-fix identique. **Poussé le 2026-07-19 — commit `6c8c913` (SW v22).**
- 2026-07-19 — **Header responsive : boutons avant l'horloge + deux-points clignotants** (retour Serge, SW `v20→v21`) : **(1)** l'horloge centrale survit **beaucoup plus longtemps** — au lieu d'être masquée dès ≤820px, on **contracte d'abord les boutons** : `✎ Edit` et `+ New` perdent leur **mot** et deviennent **icônes seules** (`✎` et `+` orange) **ensemble** à ≤800px (un seul breakpoint — d'abord staggé 800/710 puis unifié sur retour Serge « pourquoi des breakpoints différents »), l'horloge n'est masquée qu'à **≤560px**. Implémenté en scindant chaque bouton en `span.ico` + `span.lbl` (helper `setBtn()`, appliqué dans le toggle édition + `applyStatic`) → CSS masque `.lbl` seul. Le `+` orange reste compréhensible (seul bouton coloré = ajouter). **(2)** les **deux-points de l'heure clignotent** (1 Hz, look réveil digital) : horloge reconstruite en spans persistants `HH`/`:`/`MM` (seuls les chiffres sont mis à jour au tick → l'animation CSS du `:` n'est plus réinitialisée chaque seconde) ; `@keyframes clkColon` + garde `prefers-reduced-motion`. Vérifié headless : échelle de dégradation 960→430px (New/Edit se contractent bien ensemble à ~800, horloge visible jusqu'à ~560) + preuve du mécanisme de blink (phases lit/dark forcées). **Poussé le 2026-07-19 — commit `2716c08` (SW v21, groupe les lots du 19/07).**
- 2026-07-19 — **Adversarial review pré-Flutter : 4 fixes + dead code + perf** (SW reste `v21`, jamais déployé — les lots s'empilent). **Fixes** : (1) `const newBtn` déclaré (avant : reposait sur le global implicite id→window — piège à la conversion Dart) ; (2) `mSave` appelle `stopSpeechFor()` (éditer+sauver un timer qui sonne ne laisse plus parler une annonce en file) ; (3) `mPhrase.value` protégé (`t.phrase||''` — plus de « undefined » affiché sur de très vieux timers) ; (4) **bug drag→modale corrigé** : en mode édition, un drag lâché sur un gap (pas de drop) déclenchait le click natif → la modale s'ouvrait ; flag `suppressClick` armé au pointerup si `moved` (reproduit sur l'ancien code par harnais headless, corrigé sur le nouveau, tap normal intact). **Dead code retiré** : CSS `#ver` orphelin (élément disparu du markup) ; élément `.sub` complet (4 règles CSS, `<div class="sub">` du template, écritures JS à chaque tick, 4 clés i18n ×2 langues `tapStart/tapPause/paused/tapStop`). **Perf `updateTiles`** : refs sous-éléments cachées à la création (`el._t`/`el._r`, plus de `querySelector`×6 ni `timers.find` par tuile au tick 150ms) + écritures DOM **uniquement sur changement** (`put()`/`setPie()` — une tuile idle = 0 écriture/tick, ~-85 % d'écritures DOM) ; chip de zone + attrs statiques déplacés dans `render()`. Camembert `--p`/`--fill` toujours à 150ms (animation voulue). **Vérifié** : `node --check` OK ; harnais headless old-vs-new (parité : start→running, décompte, `--p` avance, banner « 3 steps », tap→pause) ; screenshots 1024/780/500 intacts. Reste (notes conversion Flutter, non appliqué) : migrations one-shot à ne pas porter, theme_color meta≠manifest, SW network-first sans timeout, `catch(()=>m)` sw.js. **Poussé — commit `2716c08`.**
- 2026-07-19 — **Modal Réglages (gear ⚙) : langue + volume** (retour Serge, SW reste `v21`) : le bouton EN/FR du header devient un **gear ⚙** qui ouvre un modal **Réglages** — toggle de langue **English/Français avec drapeaux SVG inline** (les emoji drapeaux ne s'affichent pas sous Windows) + **slider de volume** (0-100 %, pas de 5, préview sonore au relâcher) + mention bilingue « Assurez-vous que votre appareil n'est pas en mode silencieux ». Le slider pilote le **volume de l'APP** (master gain Web Audio + voix TTS `u.volume`), persistant (`sezam-vol`) — un navigateur ne peut PAS régler le volume device ; en Flutter le même slider sera branché sur le **stream alarme système** ([spec-flutter-v1 §5](../timer-app/spec-flutter-v1.md)). Overlay/skin partagés avec le modal Zones. Titres de sections agrandis (`.68rem`→`.92rem`) + séparateur entre Langue et Volume + mention silencieux agrandie (retour Serge). Vérifié : `node --check` OK + harnais headless (ouverture, bascule FR → UI traduite + `sezam-lang=fr`, slider 60 % → `sezam-vol=0.6` persistant) + screenshots. **Poussé — commit `2716c08`.**

## Déploiement
GitHub Pages, repo `sergemio/cadence-kitchen-timer` → https://sergemio.github.io/cadence-kitchen-timer/
*(repo renommé depuis `sezam-timers` le 18/07/2026 ; les clés localStorage/cache internes gardent le préfixe `sezam-` — NE PAS renommer, ça effacerait les timers des utilisateurs.)*
Pour mettre à jour : commit + push sur `main` (bumper `CACHE` dans `sw.js` à chaque changement pour forcer le refresh des clients).
